name: Usando Variáveis de Ambiente
on:
  push:
    branches: ['main']
  workflow_dispatch:
jobs:
  usando_envs:    
    runs-on: ubuntu-latest
    steps:
      - name: 'Primeira Action'
        env:
          ENV_ACTION: 'Env de Action'      
        run: |
          echo $ENV_ACTION
