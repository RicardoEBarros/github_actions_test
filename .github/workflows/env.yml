name: Usando Variáveis de Ambiente
on:
  push:
    branches: ['main']
  workflow_dispatch:
jobs:
  usando_envs:    
    runs-on: ubuntu-latest
    env:
      ENV_JOB: 'Env de Job'
    steps:
      - name: 'Primeira Action'
        run: |
          echo $ENV_JOB
      - name: 'Primeira Action'
        run: |
          echo $ENV_JOB 
