name: Usando Variáveis de Ambiente
on:
  push:
    branches: ['push']
  workflow_dispatch:
env:
  ENV_WORKFLOW: 'Aprendendo sobre Environment Variables'
jobs:
  usando_envs:
    runs-on: ubuntu-latest
    steps:
      - name: 'Primeira Action'
        run: |
          echo '$ENV_WORKFLOW'
