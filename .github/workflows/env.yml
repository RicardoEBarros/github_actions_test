name: Usando Variáveis de Ambiente
on:
  push:
    branches: ['main']
  workflow_dispatch:
jobs:
  usando_envs:    
    runs-on: ubuntu-latest
    steps:
      - name: 'Primeira Action'
        env:
          ENV_JOB: 'Env de Action'      
        run: |
          echo $ENV_JOB
